---
alwaysApply: true
---

# Scope
composer

# Directive
Reference `tasks.md` for step-by-step instructions

Reference `architecture.md` for system context

Do not execute the next task until explicitly told to do so
# Scope
composer

# Directive
Reference `tasks.md` for step-by-step instructions

Reference `architecture.md` for system context

Do not execute the next task until explicitly told to do so
